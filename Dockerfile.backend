FROM python:3.14-slim

WORKDIR /app

COPY pyproject.toml .
COPY backend/ backend/
COPY main.py .

RUN pip install --no-cache-dir .

ENV DATABASE_PATH=/app/data/novel2scenario.db

EXPOSE 8000

CMD ["uvicorn", "backend.main:app", "--host", "0.0.0.0", "--port", "8000"]
